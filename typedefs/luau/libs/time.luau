local any = nil :: any

local time = {}

--[[
	Returns the current time, in nanoseconds, since an arbitrary point.
	
	This clock is monotonic, meaning it will never reduce.
]]
function time.now(): number
	return any
end

return time
